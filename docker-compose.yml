services:
  rhybphaser:
    build: .
    image: joelnitta/rhybphaser:latest
    container_name: rhybphaser
    volumes:
      # Mount your data directory
      - ./data:/data/input
      # Mount output directory
      - ./output:/data/output
    working_dir: /data
    # Keep container running for interactive use
    stdin_open: true
    tty: true
    # Optionally set memory limits (uncomment and adjust as needed)
    # mem_limit: 8g
    # cpus: 4
