% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_mapped_reads.R
\name{extract_mapped_reads}
\alias{extract_mapped_reads}
\title{Extract Mapped Reads}
\usage{
extract_mapped_reads(
  base_dir = "./",
  output_dir = "./mapped_reads/",
  namelist = NULL,
  remove_duplicate_sequences = FALSE
)
}
\arguments{
\item{base_dir}{Path to base folder of either:
\itemize{
\item HybPhaser output (contains \verb{01_data/})
\item HybPiper-RBGV output (contains \verb{04_processed_gene_directories/})
\item HybPiper output (contains sample directories)
}}

\item{output_dir}{Path to output directory for mapped read files.
Default is \verb{./mapped_reads/}.}

\item{namelist}{Optional path to a file containing sample names, one per
line. If \code{NULL}, all sample directories are processed.}

\item{remove_duplicate_sequences}{Logical; if \code{TRUE}, deduplicate reads by
sequence (not just duplicated names).}
}
\value{
Invisibly returns a list with:
\describe{
\item{output_dir}{Output directory path}
\item{samples}{Character vector of processed samples}
\item{files}{Named character vector of output files by sample}
\item{n_samples}{Number of processed samples}
}
}
\description{
Extracts reads mapped to target sequences from HybPhaser or HybPiper
output, concatenates them per sample, and optionally deduplicates by
sequence.
}
\details{
This is an R implementation of the \verb{2_extract_mapped_reads.sh} workflow
step.
}
\examples{
\dontrun{
result <- extract_mapped_reads(
  base_dir = "hybphaser_output",
  output_dir = "mapped_reads"
)

result_dedup <- extract_mapped_reads(
  base_dir = "hybphaser_output",
  output_dir = "mapped_reads",
  namelist = "samples.txt",
  remove_duplicate_sequences = TRUE
)
}
}
