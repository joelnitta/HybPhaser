% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docker_workflows.R
\name{run_extract_mapped_reads}
\alias{run_extract_mapped_reads}
\title{Extract Mapped Reads}
\usage{
run_extract_mapped_reads(
  base_dir,
  output_dir,
  namelist = NULL,
  remove_duplicates = FALSE,
  engine = c("docker", "local"),
  docker_image = "joelnitta/rhybphaser:latest",
  pull_image = FALSE
)
}
\arguments{
\item{base_dir}{Path to base directory containing sample data (HybPiper
or HybPhaser output)}

\item{output_dir}{Path to output directory for extracted reads}

\item{namelist}{Path to file containing sample names, one per line. If
NULL, processes all samples found.}

\item{remove_duplicates}{Logical; if TRUE, remove duplicate sequences
(not just duplicate names)}

\item{engine}{One of \code{"docker"} (default) or \code{"local"}. \code{"local"} runs
the bundled bash script directly and needs a POSIX shell (not available
on native Windows).}

\item{docker_image}{Docker image name. Default is
"joelnitta/rhybphaser:latest"}

\item{pull_image}{Logical; if TRUE, pull Docker image if not found}
}
\value{
Exit code (0 for success, non-zero for failure)
}
\description{
Extracts and concatenates reads that mapped to target sequences from
HybPiper or HybPhaser output, by running a bundled bash script. By default
the script runs inside the \code{rhybphaser} Docker container; set
\code{engine = "local"} to run it directly.
}
\examples{
\dontrun{
# Extract mapped reads from all samples
run_extract_mapped_reads(
  base_dir = "path/to/hybphaser_output",
  output_dir = "path/to/mapped_reads"
)

# Extract with duplicate removal
run_extract_mapped_reads(
  base_dir = "hybphaser_output",
  output_dir = "mapped_reads",
  remove_duplicates = TRUE,
  namelist = "samples_to_process.txt"
)
}
}
