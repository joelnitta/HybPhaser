% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docker_workflows.R
\name{run_generate_consensus_sequences}
\alias{run_generate_consensus_sequences}
\title{Generate Consensus Sequences via Docker}
\usage{
run_generate_consensus_sequences(
  hybpiper_dir,
  output_dir,
  namelist = NULL,
  sample = NULL,
  intronerate = FALSE,
  cleanup = FALSE,
  threads = 1,
  min_depth = 10,
  min_allele_freq = 0.15,
  min_allele_count = 4,
  docker_image = "joelnitta/rhybphaser:latest",
  pull_image = FALSE
)
}
\arguments{
\item{hybpiper_dir}{Path to HybPiper output directory (on host machine)}

\item{output_dir}{Path to output directory for HybPhaser results (on host
machine). Will be created if it doesn't exist.}

\item{namelist}{Path to file containing sample names, one per line. If
NULL, processes all samples in hybpiper_dir.}

\item{sample}{Single sample name to process. Ignored if namelist is
provided.}

\item{intronerate}{Logical; if TRUE, use intronerated supercontigs}

\item{cleanup}{Logical; if TRUE, remove intermediate BAM and VCF files}

\item{threads}{Integer; number of CPU threads to use. Default is 1.}

\item{min_depth}{Integer; minimum read depth for variant calling.
Default is 10.}

\item{min_allele_freq}{Numeric; minimum allele frequency (0-1) for
calling heterozygous sites. Default is 0.15.}

\item{min_allele_count}{Integer; minimum number of reads supporting an
allele. Default is 4.}

\item{docker_image}{Docker image name. Default is
"joelnitta/rhybphaser:latest"}

\item{pull_image}{Logical; if TRUE, pull Docker image if not found}
}
\value{
Exit code (0 for success, non-zero for failure)
}
\description{
Generates consensus sequences from HybPiper output by remapping reads to
contigs and calling variants. Runs a bundled bash script in the
rhybphaser Docker container.
}
\examples{
\dontrun{
# Generate consensus sequences for all samples
run_generate_consensus_sequences(
  hybpiper_dir = "path/to/hybpiper_output",
  output_dir = "path/to/hybphaser_output",
  namelist = "samples.txt",
  threads = 4
)

# Process a single sample
run_generate_consensus_sequences(
  hybpiper_dir = "hybpiper_out",
  output_dir = "hybphaser_out",
  sample = "sample001"
)
}
}
