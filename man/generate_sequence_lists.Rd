% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_sequence_lists.R
\name{generate_sequence_lists}
\alias{generate_sequence_lists}
\title{Generate sequence lists for loci and samples}
\usage{
generate_sequence_lists(
  path_to_output_folder,
  fasta_file_with_targets,
  targets_file_format = "DNA",
  path_to_namelist,
  intronerated_contig = FALSE,
  subset_name = ""
)
}
\arguments{
\item{path_to_output_folder}{Path to the base HybPiper output folder
containing the "01_data" subfolder with sample results.}

\item{fasta_file_with_targets}{Path to FASTA file with target sequences
used in HybPiper run.}

\item{targets_file_format}{Character string, either "DNA" or "AA"
(amino acid).}

\item{path_to_namelist}{Path to text file with sample names (one per line)
to include in sequence lists.}

\item{intronerated_contig}{Logical. If TRUE, processes intronerated contigs
instead of regular contigs. Default FALSE.}

\item{subset_name}{Optional name for dataset optimization subset. If
provided, creates output in subfolder "03_sequence_lists_<subset_name>"
and loads R objects from corresponding assessment subfolder. If empty
string (default), uses "03_sequence_lists" and loads from
"00_R_objects".}
}
\value{
Invisibly returns a list with components:
\itemize{
\item \code{output_dir}: Path to main output directory
\item \code{loci_consensus_dir}: Path to locus consensus sequence directory
\item \code{loci_contigs_dir}: Path to locus contig sequence directory
\item \code{samples_consensus_dir}: Path to sample consensus sequence directory
\item \code{samples_contigs_dir}: Path to sample contig sequence directory
\item \code{n_loci_files}: Number of locus files created
\item \code{n_sample_files}: Number of sample files created
\item \code{loci_removed}: Character vector of loci removed from sequence lists
\item \code{samples_removed}: Character vector of samples removed from sequence lists
}
}
\description{
Creates FASTA sequence lists organized by locus and by sample, applying
quality filters from dataset optimization. Generates four sets of output
files (locus-level and sample-level, for both consensus and contig
sequences) that can be used for downstream phylogenetic analyses and
phasing.
}
\details{
This function performs the following steps:
\enumerate{
\item \strong{Load filtering results}: Reads R objects saved by \code{\link[=assess_dataset]{assess_dataset()}}:
\itemize{
\item \code{outsamples_missing}: Samples removed for missing data
\item \code{outloci_missing}: Loci removed for missing data
\item \code{outloci_para_all}: Loci removed as paralogs across all samples
\item \code{outloci_para_each}: Loci removed as paralogs for individual samples
\item \code{Table_SNPs_cleaned.Rds}: Cleaned SNP table
}
\item \strong{Generate locus-level sequence lists}: For each target locus,
concatenates sequences from all samples into a single FASTA file
(one file per locus). Creates both consensus and contig versions.
\item \strong{Generate sample-level sequence lists}: For each sample, concatenates
sequences from all loci into a single FASTA file (one file per sample).
Creates both consensus and contig versions.
\item \strong{Apply quality filters}: Removes sequences based on filters from
\code{assess_dataset()}:
\itemize{
\item Samples removed for missing data
\item Loci removed for missing data or as paralogs (all samples)
\item Loci removed as paralogs for specific samples
}
}

The function handles both Linux and non-Linux systems, using bash commands
(\code{cat}, \code{sed}) on Linux for faster processing, and R file operations
otherwise.

Generated sequences are in non-interleaved FASTA format.
}
\section{Output directories}{

Four directories are created under \verb{03_sequence_lists} (or
\verb{03_sequence_lists_<subset_name>}):
\itemize{
\item \verb{loci_consensus/}: One file per locus with consensus sequences from all
samples
\item \verb{loci_contigs/}: One file per locus with contig sequences from all
samples
\item \verb{samples_consensus/}: One file per sample with consensus sequences from
all loci
\item \verb{samples_contigs/}: One file per sample with contig sequences from all
loci
}
}

\section{File naming}{

\itemize{
\item Locus files: \verb{<locus>_consensus.fasta} or \verb{<locus>_intronerated_consensus.fasta}
\item Sample files: \verb{<sample>_consensus.fasta} or \verb{<sample>_intronerated_consensus.fasta}
\item Similar naming for contig files (with \verb{_contig} or \verb{_contigs})
}
}

\examples{
\dontrun{
# Generate sequence lists after running assess_dataset
result <- generate_sequence_lists(
  path_to_output_folder = "output",
  fasta_file_with_targets = "targets.fasta",
  targets_file_format = "DNA",
  path_to_namelist = "namelist.txt"
)

# With subset name
result <- generate_sequence_lists(
  path_to_output_folder = "output",
  fasta_file_with_targets = "targets.fasta",
  targets_file_format = "DNA",
  path_to_namelist = "namelist.txt",
  subset_name = "filtered"
)

# View results
cat("Created", result$n_loci_files, "locus files\n")
cat("Created", result$n_sample_files, "sample files\n")
}

}
\seealso{
\code{\link[=assess_dataset]{assess_dataset()}}, \code{\link[=count_snps]{count_snps()}}
}
