% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_dataset.R
\name{assess_dataset}
\alias{assess_dataset}
\title{Assess HybPhaser dataset quality}
\usage{
assess_dataset(
  snp_table,
  length_table,
  targets_file,
  targets_type = c("DNA", "AA"),
  output_dir,
  subset_name = "",
  min_loci_per_sample_prop = 0.5,
  min_target_length_per_sample_prop = 0.5,
  min_samples_per_locus_prop = 0.5,
  min_target_length_per_locus_prop = 0.5,
  paralog_threshold = "none",
  paralog_file = NULL,
  remove_sample_outliers = FALSE
)
}
\arguments{
\item{snp_table}{Path to RDS file containing SNP proportions table
(output from \code{\link[=count_snps]{count_snps()}}), or a matrix/data.frame with loci as rows
and samples as columns.}

\item{length_table}{Path to RDS file containing sequence length table
(output from \code{\link[=count_snps]{count_snps()}}), or a matrix/data.frame with loci as rows
and samples as columns.}

\item{targets_file}{Path to FASTA file with target sequences.}

\item{targets_type}{Character string, either "DNA" or "AA" (amino acid).
If "AA", lengths will be multiplied by 3 to get nucleotide length.}

\item{output_dir}{Path to output directory where assessment results will
be saved.}

\item{subset_name}{Optional name for dataset optimization subset. If
provided, creates subfolder "02_assessment_<subset_name>". If empty
string (default), creates "02_assessment" folder.}

\item{min_loci_per_sample_prop}{Minimum proportion of loci that must be
recovered per sample (default: 0.5). Samples below this threshold are
removed.}

\item{min_target_length_per_sample_prop}{Minimum proportion of target
sequence length that must be recovered per sample (default: 0.5).
Samples below this threshold are removed.}

\item{min_samples_per_locus_prop}{Minimum proportion of samples that must
recover a locus (default: 0.5). Loci below this threshold are removed.}

\item{min_target_length_per_locus_prop}{Minimum proportion of target
sequence length that must be recovered per locus (default: 0.5).
Loci below this threshold are removed.}

\item{paralog_threshold}{Threshold for removing putative paralog loci
across all samples. Can be:
\itemize{
\item Numeric: remove loci with mean SNP proportion above this value
\item "outliers": use 1.5*IQR method to identify outliers
\item "file": read list of loci to remove from \code{paralog_file}
\item "none" or NA: skip paralog removal (default)
}}

\item{paralog_file}{Path to text file with list of paralog loci to remove
(one per line). Only used if \code{paralog_threshold = "file"}.}

\item{remove_sample_outliers}{Logical. If TRUE, removes outlier loci for
each sample individually using 1.5*IQR method (default: FALSE).}
}
\value{
Invisibly returns a list with components:
\itemize{
\item \code{snp_table_cleaned}: Cleaned SNP table after filtering
\item \code{length_table_cleaned}: Cleaned length table after filtering
\item \code{samples_removed}: Character vector of removed sample names
\item \code{loci_removed_missing}: Character vector of loci removed for missing
data
\item \code{loci_removed_paralogs_all}: Character vector of loci removed as
paralogs across all samples
\item \code{loci_removed_paralogs_each}: Named list of loci removed per sample
\item \code{summary_table}: Data frame with heterozygosity and allele divergence
metrics per sample
}
}
\description{
Generate tables and graphs to assess sequence recovery, missing data,
and identify putative paralogs based on SNP proportions. This function
applies quality filters and produces comprehensive assessment reports.
}
\details{
The function performs three main assessment steps:

\strong{Step 1: Missing Data Assessment}
\itemize{
\item Calculates proportion of loci recovered per sample
\item Calculates proportion of samples recovering each locus
\item Calculates proportion of target sequence length recovered
\item Removes samples and loci failing to meet minimum thresholds
\item Generates plots and tables showing data recovery
}

\strong{Step 2a: Paralog Detection (All Samples)}
\itemize{
\item Identifies loci with unusually high SNP proportions across all samples
\item Applies specified threshold method
\item Generates barplots showing mean SNP proportions per locus
}

\strong{Step 2b: Paralog Detection (Per Sample)}
\itemize{
\item Optionally identifies outlier loci for each sample individually
\item Uses 1.5*IQR method on loci with SNPs
\item Generates boxplots showing SNP distributions per sample
}

\strong{Step 3: Heterozygosity and Allele Divergence}
\itemize{
\item Calculates locus heterozygosity (proportion of loci with SNPs)
\item Calculates allele divergence (weighted mean SNP proportion)
\item Generates summary table and scatter plots
}

Output files are saved to \verb{<output_dir>/02_assessment[_<subset_name>]/}:
\itemize{
\item \verb{1_Data_recovered_overview.pdf/png}: Missing data plots
\item \verb{1_Data_recovered_per_sample.csv}: Recovery metrics per sample
\item \verb{1_Data_recovered_per_locus.csv}: Recovery metrics per locus
\item \verb{1_Summary_missing_data.txt}: Text summary of removed samples/loci
\item \verb{2a_Paralogs_for_all_samples.pdf/png}: Paralog detection plots
\item \verb{2a_List_of_paralogs_removed_for_all_samples.txt}: List of removed loci
\item \verb{2b_Paralogs_for_each_sample.pdf/png}: Per-sample outlier plots
\item \verb{2_Summary_Paralogs.txt}: Text summary of paralog removal
\item \verb{3_LH_vs_AD.pdf/png}: Locus heterozygosity vs allele divergence plot
\item \verb{3_varLH_vs_AD.pdf/png}: Multiple heterozygosity threshold plots
\item \verb{4_Summary_table.csv}: Final metrics per sample
\item \verb{0_Table_SNPs.csv}: Cleaned SNP table
\item \verb{0_Table_consensus_length.csv}: Cleaned length table
\item \verb{0_namelist_included_samples.txt}: List of retained samples
}

R objects are also saved to \verb{<output_dir>/00_R_objects/<subset_name>/}:
\itemize{
\item \code{Table_SNPs_cleaned.Rds}
\item \code{Table_consensus_length_cleaned.Rds}
\item \code{Summary_table.Rds}
\item Various vectors of removed samples/loci
}
}
\examples{
\dontrun{
# Run count_snps first
snp_results <- count_snps(
  consensus_dir = "01_seqs",
  output_dir = "output"
)

# Then assess dataset
assess_dataset(
  snp_table = file.path("output", "00_R_objects", "Table_SNPs.Rds"),
  length_table = file.path("output", "00_R_objects",
    "Table_consensus_length.Rds"),
  targets_file = "targets.fasta",
  targets_type = "DNA",
  output_dir = "output",
  min_loci_per_sample_prop = 0.5,
  min_samples_per_locus_prop = 0.5,
  paralog_threshold = "outliers",
  remove_sample_outliers = TRUE
)
}

}
